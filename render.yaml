services:
  - type: web
    name: road-sos-ml-backend
    runtime: python
    plan: free
    rootDir: ml-backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
